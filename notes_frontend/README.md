# Notes Frontend

A simple React notes app with:

- Create, edit, delete notes
- Search by title/content/tags
- Tag filter chips (derived from saved notes)
- Local persistence via `localStorage`
- Responsive minimal light UI

## Run

```bash
npm install
npm start
```

The app uses `REACT_APP_PORT` (default: 3000).
